# 胡晰瑞模块说明与指标

## 模块范围

本模块补全胡晰瑞负责的两项工作：

1. 身体部位划分：输出肩部、背部、腰部、臀部、大腿、小腿六个区域的行列边界、平均压力、最大压力和负载占比。
2. 体重预测：输出连续体重预测值、七个体重区间之一、预测来源和空载校正状态。

实现文件为 `body_weight.py`，已接入 `server.py` 的 `/api/replay` 接口，并在前端热力图上叠加区域框。

## 方法

- 区域划分优先使用本地课程 `区域划分/data.json` 中的人工标注样本，提取压力总载荷、接触面积、最大压力、平均压力和压力中心作为特征，通过标准化 KNN 选择最接近的区域模板。
- 如果本地没有区域标注 JSON，则退回到压力有效轮廓法，按人体纵向比例估计六个部位。
- 体重预测使用用户级压力特征做岭回归标定，特征包括总载荷平方根、最大压力、按身高校正的载荷和压力中心。
- 空载校正读取每个用户目录下的 `*_空载.txt`，对推理帧做逐点基线扣除；没有空载文件时保留未校正结果并在接口中标明。
- 原始数据、身高体重表和模型大文件不提交到仓库；运行时从本地数据目录读取。

## 体重区间

| index | 区间 |
| --- | --- |
| 0 | 45-55 kg |
| 1 | 55-65 kg |
| 2 | 65-75 kg |
| 3 | 75-85 kg |
| 4 | 85-95 kg |
| 5 | 95-105 kg |
| 6 | 105 kg以上 |

## 复现命令

PowerShell：

```powershell
$env:MATTRESS_DATA_DIR = 'D:\智能床垫\睡姿 区域划分data\睡姿数据'
python scripts\evaluate_body_weight.py
python server.py
```

如果数据目录不在仓库同级位置，可显式指定：

```powershell
python scripts\evaluate_body_weight.py --data-root 'D:\智能床垫\睡姿 区域划分data'
```

## 当前指标

本地课程数据评估结果：

```json
{
  "region": {
    "known_users": ["SAI", "dgs"],
    "known_user_parts": 8640,
    "known_user_accuracy": 100.0,
    "test_users": ["gzy"],
    "parts": 4320,
    "new_user_accuracy": 77.64
  },
  "weight": {
    "test_users": 10,
    "mae_kg": 4.98,
    "interval_or_adjacent_hit_rate": 100.0,
    "far_miss_rate": 0.0
  }
}
```

说明：区域 JSON 中只有 `SAI`、`dgs`、`gzy` 三名用户存在完整区域标注，其余记录区域字段为 `na`，因此区域新用户测试使用 `gzy`。体重预测使用睡姿数据中 33 名用户，按用户名排序做 70% 训练、30% 新用户测试。
